"use client"

import { UserType } from "@/lib/types"

const AboutMe = ({firstname, lastname, email, profileImageUrl, phoneNumber}: UserType) => {

  return (
    <section>
      <div>
        detail of user
      </div>
      <div>
                
      </div>
    </section>
  )
}

// addString and subString func 
export default AboutMe