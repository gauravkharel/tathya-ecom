import CartLayout from '@/components/cart/CartLayout'
import { FC } from 'react'

interface pageProps {
  
}

const page: FC<pageProps> = ({}) => {
  return <div>
    <CartLayout />
  </div>
}

export default page