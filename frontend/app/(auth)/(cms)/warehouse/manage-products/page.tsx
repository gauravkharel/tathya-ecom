import { AddProductForm } from '@/components/admin/product/AddProduct'
import { FC } from 'react' 

interface pageProps {
  
}

const page: FC<pageProps> = ({}) => {
  return <div><AddProductForm />
  </div>
}

export default page